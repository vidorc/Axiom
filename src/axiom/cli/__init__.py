"""CLI delivery layer (`axiom`).

The CLI is the ONLY user surface in Phase 1 (PHASE_1.md WS-6) — it forces the API
and engine contracts to be clean and scriptable before any UI papers over them.
One of three run modes of the single axiom package (ADR-0002).
"""
